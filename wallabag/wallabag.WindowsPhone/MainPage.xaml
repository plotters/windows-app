<Page
    x:Class="wallabag.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:wallabag.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding DefaultViewModel, Mode=TwoWay, RelativeSource={RelativeSource Mode=Self}}">
    <Page.Resources>
        <DataTemplate x:Key="ArticleTemplate">
            <Grid>
                <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" Style="{StaticResource ListViewItemTextBlockStyle}"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Uid="settingsButton" Label="settings" Command="{Binding openSettingsCommand}"/>
            </CommandBar.SecondaryCommands>
            <AppBarButton x:Uid="addItemButton" Icon="Add" IsCompact="True" Label="add" Command="{Binding addLinkCommand}" Visibility="{Binding AddLinkButtonVisibility, Mode=OneWay}"/>
            <AppBarButton x:Uid="refreshButton" Icon="Refresh" IsCompact="True" Label="refresh" Command="{Binding refreshCommand}"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Pivot x:Name="mainPivot" Title="WALLABAG" Grid.RowSpan="2">
            <PivotItem x:Uid="unread" Header="unread">
                <ListBox ItemsSource="{Binding unreadItems}" ItemTemplate="{StaticResource ArticleTemplate}" Background="{x:Null}" SelectionChanged="ListBox_SelectionChanged" />
            </PivotItem>
            <PivotItem x:Uid="favourites" Header="favourites">
                <ListBox ItemsSource="{Binding favouriteItems}" ItemTemplate="{StaticResource ArticleTemplate}" Background="{x:Null}" SelectionChanged="ListBox_SelectionChanged" />
            </PivotItem>
            <PivotItem x:Uid="archive" Header="archive">
                <ListBox ItemsSource="{Binding archivedItems}" ItemTemplate="{StaticResource ArticleTemplate}" Background="{x:Null}" SelectionChanged="ListBox_SelectionChanged" />
            </PivotItem>
        </Pivot>
        <ProgressBar VerticalAlignment="Top" IsIndeterminate="{Binding IsLoading}"/>
    </Grid>
</Page>
