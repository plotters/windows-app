<ContentDialog
    x:Class="wallabag.Views.AddLink"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wallabag.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="saveLinkDialog"
    Title="SAVE LINK"
    PrimaryButtonText="close" FullSizeDesired="True">

    <Grid>
        <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <TextBox Name="link" Header="URL" InputScope="Url" PlaceholderText="http://"/>
            <Button x:Uid="validateUrlButton" Content="validate" Click="ContentDialog_PrimaryButtonClick" />
            <WebView x:Name="webView" MinHeight="400"/>
        </StackPanel>
    </Grid>
</ContentDialog>
